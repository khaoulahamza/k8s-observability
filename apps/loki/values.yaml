commonConfig:
  replication_factor: 1

storage:
  type: filesystem

auth_enabled: false

singleBinary:
  replicas: 1
  persistence:
    enabled: true
    size: 10Gi

test:
  enabled: false

monitoring:
  selfMonitoring:
    enabled: false
  serviceMonitor:
    enabled: false

gateway:
  enabled: true
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/rewrite-target: /
      nginx.ingress.kubernetes.io/proxy-buffering: "off"
    hosts:
      - host: loki.staging.local
        paths:
          - path: /
            pathType: Prefix

# ✅ PROMTAIL ACTIVÉ POUR COLLECTER LES LOGS
promtail:
  enabled: true
  config:
    clients:
      - url: http://loki-gateway.observability.svc:80/loki/api/v1/push
